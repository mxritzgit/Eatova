<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- PROD-1 (flutter_local_notifications): POST_NOTIFICATIONS drives the
         Android 13+ runtime dialog. Only inexact alarms are scheduled
         (AndroidScheduleMode.inexactAllowWhileIdle), so no
         SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM and no Play policy review.
         RECEIVE_BOOT_COMPLETED lets scheduled nudges survive a reboot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- E1: camera_android_camerax declares RECORD_AUDIO and
         WRITE_EXTERNAL_STORAGE for video capture; the merger derives
         READ_EXTERNAL_STORAGE without maxSdkVersion from the latter, a real
         runtime permission on API 29-32. Eatova only takes stills and picks
         gallery images via image_picker (system photo picker, no storage
         permission needed). Coach speech runs over the `eatova/speech`
         MethodChannel, registered on iOS only, so there is no Android audio
         path. All three are removed. -->
    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />

    <!-- P10-06: androidx.biometric:1.1.0 declares both of these in its AAR
         manifest and rides in transitively via google_sign_in_android ->
         androidx.credentials, so the merged manifest advertised biometric and
         fingerprint hardware for an app that has no biometric feature at all:
         no local_auth in pubspec.lock, and flutter_secure_storage
         deliberately leaves setUserAuthenticationRequired unset (see
         secure_cache_store.dart).

         Checked before removing, because Credential Manager CAN raise a
         biometric prompt: in androidx.credentials-1.6.0 every class that
         references androidx.biometric.BiometricPrompt lives in
         androidx.credentials.provider.* (BiometricPromptData, the
         *Entry$Api35Impl classes, CryptoObjectUtils) — the API for apps that
         PROVIDE credentials. Eatova only consumes one (GetGoogleIdOption via
         google_sign_in), credentials-play-services-auth references
         androidx.biometric nowhere, and its account sheet is drawn by Play
         services in ANOTHER process, where this manifest does not apply.

         Residual risk, documented on purpose: if sign-in ever moves to
         passkeys or a provider returns BiometricPromptData, the in-process
         BiometricPrompt needs USE_BIOMETRIC back. The symptom would be a
         SecurityException on the Google login path, so a device sign-in is
         the check after changing anything in that area. -->
    <uses-permission
        android:name="android.permission.USE_BIOMETRIC"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.USE_FINGERPRINT"
        tools:node="remove" />

    <application
        android:label="Eatova"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:replace="android:allowBackup,android:dataExtractionRules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="eatova"
                    android:host="login-callback" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- D1: flutter_local_notifications ships no components of its own, so
             the app must declare them. ScheduledNotificationReceiver is the
             target of every scheduled alarm; without this line Android drops
             the broadcast silently — no crash, no notification, ever. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- flutter_local_notifications: reschedules local nudges after a
             reboot. Needs the explicit intent filter to fire. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Deliberately not declared, because nothing uses them:
             ActionBroadcastReceiver (notification actions / dismissIsolate) and
             ForegroundService (startForegroundService). Adding either feature
             requires declaring its component here, or it fails silently like
             D1. -->

        <!-- E4: package:health injects this service exported="true" without
             android:permission — the only unguarded exported component besides
             the launcher activity. Health Connect is unreachable on Android
             (apple_health_service.dart bails on `if (!Platform.isIOS)`), so the
             service has no caller and is removed. Cleaner fix would be dropping
             `health` from the Android build in pubspec.yaml. -->
        <service
            android:name="androidx.health.platform.client.impl.sdkservice.HealthDataSdkService"
            tools:node="remove" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
